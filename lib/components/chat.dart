class Chat{
  final String name, lastMessage, image, time;
  final bool isActive;

  Chat({required this.image, required this.isActive, required this.lastMessage, required this.name, required this.time});
}